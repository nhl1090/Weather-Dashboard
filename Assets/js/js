//API key
const apiKey = 

let lastSearch =

const Form =
const Input =

let city;

const queryUrl = 

fetch(queryUrl)
    .then(function () {
    return
)}
    .then(function

    )